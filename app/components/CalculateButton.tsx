'use client';
import React from 'react'

const Calculate = () => {
  return (
    <div>
        <button onClick={() => console.log('working')}>Calculate</button>
    </div>
  )
}

export default Calculate